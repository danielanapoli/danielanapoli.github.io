import { CardGroup } from "react-bootstrap";

export function CustomCardGroup({ CustomCard }) {

    return(
        <CardGroup>
            {CustomCard}
        </CardGroup>
    );      
}

export default CustomCardGroup;
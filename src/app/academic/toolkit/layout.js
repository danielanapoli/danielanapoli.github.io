export const metadata = {
  title: 'Remote Healthcare Design Toolkit',
  description: 'UX toolkit for designing remote healthcare technology for older adults. Fifteen research-backed tools grounded in survey and interview data on privacy needs.',
  alternates: { canonical: '/academic/toolkit' },
};

export default function Layout({ children }) {
  return children;
}

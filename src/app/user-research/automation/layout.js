export const metadata = {
  title: 'Persona Integration',
  description: 'Persona integration and automation for product teams. Translating user research into classification systems and workflows that scale across departments.',
};

export default function Layout({ children }) {
  return children;
}

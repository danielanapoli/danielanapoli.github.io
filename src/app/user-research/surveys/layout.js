export const metadata = {
  title: 'Large-Scale User Surveys',
  description: 'Large-scale UX survey research for product teams. Quantitative studies using SUS, SUPR-Q, and NPS to drive confident, data-backed design decisions.',
};

export default function Layout({ children }) {
  return children;
}

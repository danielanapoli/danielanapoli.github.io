export const metadata = {
  title: 'Case Study | Large-Scale Surveys',
  description: 'A national mixed-methods survey of 384 adults and a Python ETL pipeline built to withstand fraudulent responses, turned into a validated design toolkit for remote healthcare technology.',
  alternates: { canonical: '/user-research/surveys/case-study' },
};

export default function Layout({ children }) {
  return children;
}

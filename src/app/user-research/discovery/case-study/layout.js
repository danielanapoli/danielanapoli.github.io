export const metadata = {
  title: 'Case Study | Requirements Discovery',
  description: 'Survey, 20 user interviews, and concept testing to inform CIHI\'s strategy for a new healthcare data product — before the design direction was set.',
  alternates: { canonical: '/user-research/discovery/case-study' },
};

export default function Layout({ children }) {
  return children;
}
